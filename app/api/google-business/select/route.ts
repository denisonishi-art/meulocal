import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';

export async function POST(request:Request){const auth=request.headers.get('authorization');const supabaseUrl=process.env.NEXT_PUBLIC_SUPABASE_URL,anonKey=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!auth?.startsWith('Bearer ')||!supabaseUrl||!anonKey||!serviceKey)return NextResponse.json({error:'Não autorizado.'},{status:401});const userClient=createClient(supabaseUrl,anonKey,{global:{headers:{Authorization:auth}}});const {data:{user}}=await userClient.auth.getUser(auth.slice(7));if(!user)return NextResponse.json({error:'Sessão inválida.'},{status:401});const {accountId,locationId,title}=await request.json();if(!accountId||!locationId)return NextResponse.json({error:'Selecione uma unidade.'},{status:400});const admin=createClient(supabaseUrl,serviceKey);const {error}=await admin.from('google_business_connections').update({google_account_id:accountId,google_location_id:locationId,google_location_name:title||null,status:'connected',connected_at:new Date().toISOString()}).eq('user_id',user.id);if(error)return NextResponse.json({error:'Não foi possível concluir a conexão.'},{status:500});return NextResponse.json({ok:true});}
